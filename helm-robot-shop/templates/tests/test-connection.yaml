apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-robot-shop.fullname" . }}-test-connection"
  labels:
{{ include "helm-robot-shop.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "helm-robot-shop.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
